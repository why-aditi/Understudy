An LLM calling a recorded capability
==============================================================================

model    : openai/gpt-oss-120b (Groq free tier)
catalog  : 2 approved capabilit(ies), generated from the artifacts

  tool  member.savings_balance.read  v1.0.0
        Read the current savings balance from an open sub-account screen.
        args    {}
        required []
  tool  member.search  v1.0.0
        Search for a member by id and read the name on the results row.
        args    {"member_id": {"type": "string", "description": "The member's identifier as printed on their statement.", "examples": ["12345"]}}
        required ['member_id']

------------------------------------------------------------------------------
user  : "Who is member 12345?"
model : calls member.search({"member_id": "12345"})
replay: status=success  steps=3
        outputs={"member_name": "Wilhelmina Okonkwo-Bright"}
        locators={"enter-id": "role_name", "submit-search": "role_name", "read-name": "role_name"}
        model calls during replay: 0
model : "Member 12345 is Wilhelmina Okonkwo‑Bright."

------------------------------------------------------------------------------
user  : "And can you look up member 99999 as well?"
model : calls member.search({"member_id": "99999"})
replay: status=business_outcome  steps=2
        outputs=null
        outcome=member_not_found (business)
        locators={"enter-id": "role_name", "submit-search": "role_name"}
        model calls during replay: 0
model : "Member 99999 was not found."

------------------------------------------------------------------------------
What the catalog refuses, checked without a model:

  listed as callable                 2 approved, 0 once approval is withdrawn
  a capability that does not exist   UnknownTool: no capability named 'member.teleport'; have ['member.savings_balance.read', 'member.search']
  a misspelled argument              CatalogError: member.search: unknown parameter(s) ['membre_id']; expected ['member_id']
  a missing required argument        CatalogError: member.search: missing required parameter(s) ['member_id']
  calling an unapproved capability   NotApproved: 'member.search' is not callable: state=draft, outcomes_reviewed=True. A human has to approve it and review its outcomes first.

------------------------------------------------------------------------------
4 model calls in total, all of them to choose a tool or phrase an answer.
Zero inside any replay - that column is the whole point of the artifact.
The model chose the capability and the argument. It decided no action within one.
